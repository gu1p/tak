//! 1. Start from the closest example and keep intent next to the source with `task(doc=...)`, crate docs, and command doc comments.
//! 2. Use `tak list`, `tak explain`, and `tak graph` to validate labels and graph shape before you run expensive work.
//! 3. Add only the execution, retry, coordination, and remote constructs the project actually needs.
